data:
  data_path: "data/raw"
  image_size: 256
  keep_aspect_ratio: false
  batch_size: 16
  clahe: true
  augment: true

model:
  channels: [32, 64, 128]
  dense_units: 64
  use_gender: true

training:
  epochs: 70
  patience: 10
  learning_rate: 0.005
  loss: "huber"
  results_csv: "experiments/train_results_summary.csv"
